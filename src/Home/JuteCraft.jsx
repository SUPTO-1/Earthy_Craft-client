const JuteCraft = ({craft}) => {
    const {photo,itemName,subcategoryName,price} = craft;
    return (
        <div>
            <h2>Name: {itemName}</h2>
        </div>
    );
};

export default JuteCraft;